name: 02 - Workeflow evants

on:
  push:
  pull_request:
  schedule:
    - cron: "0 9 * * *"     # codziennie o 09:00 UTC
  workflow_dispatch:

jobs:
  echo:
    name: "Zobaczy sobie co mnie strigerowalo ${{ github.event_name }} ${GITHUB_EVENT_NAME}"
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger ${{ github.event_name }} ${GITHUB_EVENT_NAME}
        run: |
          echo "Event name: ${{ github.event_name }}"
          echo "Event name: ---> jest dobiro podstawine ${GITHUB_EVENT_NAME}"
          env
